export class Ingredient {
    constructor(public name: string, public amount: number ){} //it will automatically create 'name' and 'amount' variable for class and automatically assign passed value to both variables.
}